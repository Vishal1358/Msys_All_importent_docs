#include<stdio.h>

int main(){
    int i = 0;

    // While ---> Checks the condition and then executes the code
    while (i<=20)
    {
        if (i>=12){
            printf("The value of i is %d\n",i);
        }
        i++;
    }
        
    return 0;
}